dom-module(id="component-app")
  template
    component-header(navigation="{{navigation}}")
    component-navigation
    component-gallery(works="{{works}}")
    component-works(works="{{works}}")
    component-footer 